# Developer Synthetic Fixtures

> [!NOTE]
> **NOT USED FOR OFFICIAL RESULTS OR BENCHMARKS.**

This directory contains lightweight synthetic developer fixtures (243 rows) used strictly for unit testing and offline frontend development when `TRACE_MODE=dev`.

Official benchmark results and investigations operate exclusively on `data/competition/benchmark_subgraphs.json` and the IEEE-CIS transaction dataset under `TRACE_MODE=competition`.
